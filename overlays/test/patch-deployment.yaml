apiVersion: apps/v1
kind: Deployment
metadata:
  name: app1
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: nginx
          image: nginx:1.18.0 # Puedes especificar una versión diferente para test
